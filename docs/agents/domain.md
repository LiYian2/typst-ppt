# Domain docs

This is a single-context repository.

Before changing a domain area, read root `CONTEXT.md` and relevant ADRs
under `docs/adr/`. Use glossary terminology consistently and explicitly
flag any proposal that conflicts with an accepted ADR. Missing domain
documents are created lazily as decisions become concrete.
