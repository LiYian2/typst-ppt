{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the presenter and audience windows",
  "windows": ["main", "audience"],
  "permissions": [
    "core:default",
    "dialog:allow-open"
  ]
}
